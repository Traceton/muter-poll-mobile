import { API } from "@env";

export let checkIfDropBoxIdIsValid = (dropBoxId) => {
  return true;
};

export let checkIfDropBoxIdAndPasswordIsValid = (
  dropBoxId,
  dropBoxPassword
) => {
  return true;
};

export let createNewDropBox = (
  dropBoxId,
  dropBoxName,
  dropBoxPassword,
  dropBoxLocation
) => {
  return true;
};
